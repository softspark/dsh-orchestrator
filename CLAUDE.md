# DSH Orchestrator

Read and follow `AGENTS.md` before changing this repository.

This module is configuration only. It composes existing DSH providers and tools; it does not own vendor authentication, model HTTP clients, credential storage, or the active DSH profile. Keep all tests deterministic and network-free.

Generated toolkit files are managed by ai-toolkit. Never hand-edit `.claude/constitution.md`.
